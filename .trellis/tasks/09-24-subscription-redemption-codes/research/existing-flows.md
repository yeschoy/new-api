# Existing flows and verification constraints (2026-09-24)

- `model/redemption.go`: `Redemption` has `Quota` only; `Redeem` acquires user quota mutation fences before DB transaction, row-locks code, checks enabled/expiry, CAS-updates status, credits wallet inside same transaction, syncs cache and logs afterward. `Insert`/`Update` currently require positive quota; `Update` always writes status.
- `controller/redemption.go`: create validates name/count/quota/expiry and inserts each code; update supports `status_only`; no plan data. `controller/user.go:TopUp` returns numeric `data` and masks redeem failures, but logs the submitted raw key on error (avoid retaining this when modifying path).
- `model/subscription.go`: `CreateUserSubscriptionFromPlanTx` enforces `MaxPurchasePerUser`, computes duration/reset/quota from current plan, and may upgrade group. `AdminBindSubscription` uses the same transactional creator but loads the plan via cache and does not check `Enabled`. `PurchaseSubscriptionWithBalance` checks `Enabled` and deducts wallet. `getSubscriptionPlanByIdTx` may return cached plan even with tx; use an explicit transactional read for the new disabled-plan gate.
- `model/main.go:migrateDB` AutoMigrates both `Redemption` and subscription tables. Latest release tag is `v1.0.0-rc.40`; model test patterns for dialect connections/version logging exist in `model/channel_settings_test.go`. Existing `model/redemption_test.go` covers quota single-use, overflow, concurrency and quota-fence ordering; reuse and extend. The admin form has a preexisting test file `web/src/features/redemption-codes/components/__tests__/redemptions-mutate-drawer.test.tsx`.
- `router/api-router.go` exposes `/api/user/topup` plus `/api/subscription/admin/plans`. Frontend `web/src/features/wallet/hooks/use-redemption.ts` expects numeric quota; admin drawer/columns/mobile/export under `web/src/features/redemption-codes/`, and existing plan client `web/src/features/subscriptions/api.ts:getAdminPlans` returns all plans. Existing reusable UI: `web/src/components/ui/combobox.tsx` used in `web/src/features/subscriptions/components/dialogs/user-subscriptions-dialog.tsx`; admin drawer and export dialog already exist.
- Local validation preflight: `docker` binary exists but daemon connection failed; `TEST_MYSQL_DSN` and `TEST_POSTGRES_DSN` unset. Real SQLite/MySQL/PostgreSQL fresh/upgrade/repeat matrix is mandatory for database changes (AGENTS.md). Try enabling a test container/runtime or getting isolated test DSNs during execution; if unavailable, disclose blocker rather than claim complete.
- Task directory is ignored by `.gitignore:48` (`.trellis/`); explicitly force-add the planning artifacts on the task's commit, per project instruction that Trellis changes must be committed. Exclude `.pi/npm/node_modules/` (unrelated untracked dependency directory).
- Implementation discovery: `CreateUserSubscriptionFromPlanTx` used `GetDBTimestamp()` on the global DB inside a transaction; with SQLite's one-connection test DB this deadlocks. The creator now reads time on `tx` via `getDBTimestampOn(tx)`.
- Validation (2026-09-24, implementation): SQLite 3.50.4 fresh and partial legacy redemption schema AutoMigrate twice, old code data/unique key and subscription zero quota pass. `go build ./...`, focused model tests, frontend typecheck, affected Vitest tests, changed-file oxlint (one preexisting warning), and web build pass. Full `go test ./model ./controller`: model passes; controller fails in unrelated OAuth/domain login, registration, and quota-cache tests.
- Check-agent review (2026-09-24): Locked the enabled plan through redeem/issue/update commit; preserved `quota default:100` so AutoMigrate does not change an existing column, writing subscription quota zero in the same insert transaction. Status-only updates now preserve concurrent entitlement/metadata edits; unused unsafe `SelectUpdate` removed. Switched transactional PostgreSQL time to `clock_timestamp()` (NOW is transaction-start time). Frontend successful grants remain success even if follow-up user refresh fails. Migration fixture now mirrors every persistent `Redemption` field/tag from release `v1.0.0-rc.40` and checks quota default, indexes, old rows/key uniqueness, and repeat migration; it is a schema-equivalent fixture, not a database physically migrated by the released binary.
- Final check commands/results: `go test ./model -run 'Test(RedemptionMigration|RedemptionStatusOnlyPreservesConcurrentEntitlement|SubscriptionRedemption|Redeem)' -count=1 -timeout=90s` PASS; `go test ./controller -run 'Test(TopUp|Redemption)' -count=1 -timeout=90s` PASS; `go build ./...` PASS; SQLite migration reports SQLite 3.50.4 PASS, MySQL and PostgreSQL SKIP due unset `TEST_MYSQL_DSN`/`TEST_POSTGRES_DSN`. `cd web && bun run i18n:sync`, `bun run typecheck`, impacted Vitest (3 files/25 tests), `bun run build`, changed-file oxlint (one warning in redemptions-columns.tsx), and `git diff --check` PASS. Full `go test ./model ./controller -count=1 -timeout=180s`: model PASS, controller FAIL in OAuth/domain login, registration and quota-cache tests (same unrelated failures).
- Docker 28.2.2 daemon responds to `docker info`, and `docker pull mysql:8.0` / `docker pull postgres:16` succeeded, but `docker start trellis-redemption-mysql` and `docker run ... postgres:16` both timed out leaving containers in `Created` state; even `docker run --rm --network none redis:7-alpine echo ok` timed out. Real MySQL/PostgreSQL fresh/upgrade/repeat migration and transaction matrix remain blocked; neither multi-database compatibility nor full completion has been verified. Containers/images and `web/scripts/add-missing-keys-task.mjs` were retained pending user cleanup consent. Parent follow-up: `docker start trellis-redemption-postgres` timed out after 90s and remained Created; `docker desktop restart` timed out after 120s, leaving daemon unreachable; `docker desktop start` says already running but status cannot be retrieved. Parent re-ran targeted `go test ./model -run 'Test(RedemptionMigration|RedemptionStatusOnlyPreservesConcurrentEntitlement|SubscriptionRedemption|Redeem)' -count=1 -timeout=120s`, `go test ./controller -run 'Test(TopUp|Redemption)' -count=1 -timeout=120s`, `go build ./...`, `cd web && bun run typecheck` and three affected Vitest files (25 tests): all PASS. Full `go test ./controller -count=1 -timeout=180s` still fails in unrelated OAuth/domain login, registration, security-account and quota-cache tests. Docker must be repaired or isolated MySQL/PostgreSQL test DSNs supplied before the three-database gate can be completed.
- Follow-up 2026-09-24: Docker container start still timed out, so `HOMEBREW_NO_AUTO_UPDATE=1 brew install mysql@8.0 postgresql@16` installed isolated local engines (and upgraded shared Homebrew formula dependencies). Test servers use a private `/tmp/newapi-redemption-db.*` directory and Unix sockets only (`mysqld --skip-networking`, PostgreSQL `listen_addresses=''`). Versions: SQLite 3.50.4, MySQL 8.0.46, PostgreSQL 16.15. `TEST_MYSQL_DSN=root@unix(<private>/mysql.sock)/trellis_redemption_test?charset=utf8mb4&parseTime=True&loc=Local`, `TEST_POSTGRES_DSN=postgres://<local-user>@localhost/trellis_redemption_test?host=<private>&sslmode=disable`; never use production DB.
- `go test ./model -run '^TestRedemptionMigration$' -count=1 -timeout=120s -v` first exposed test SQL using unquoted reserved `key` on MySQL; switched the three matrix queries to GORM map predicates; reran PASS on all three databases. Added `TestRedemptionDatabaseMatrix` in `model/redemption_test.go`: real fresh-database quota and subscription redemption, disabled/re-enabled plan, current plan value, wallet isolation, duplicate and concurrent grants on each dialect. `go test ./model -run '^TestRedemptionDatabaseMatrix$' -count=1 -timeout=120s -v` PASS on all three. Full `go test ./model -count=1 -timeout=240s` PASS with local MySQL/PostgreSQL DSNs when PostgreSQL is supplied as `postgres://` URL (project `chooseDB` treats non-URL PostgreSQL DSN as MySQL). `go test ./controller -run 'Test(TopUp|Redemption)' -count=1 -timeout=120s` PASS, `go build ./...` PASS. `cd web && bun run typecheck`, 3 affected Vitest files (25 tests) and `bun run build` PASS.
- Actual released-schema upgrade: `git archive v1.0.0-rc.40` to `/tmp/newapi-rc40.*`, ran its `model.Redemption` `AutoMigrate` plus a legacy quota code into separate SQLite/MySQL/PostgreSQL databases (`go test ./model -run '^TestReleaseRedemptionSchema$'` inside release archive), then current `AutoMigrate(&model.Redemption{})` **twice** on each (`go run /tmp/newapi-redemption-upgrade-check.go`). PASS: old ID/quota/status, quota default 100, unique key, and new plan_id default 0 remain; newly inserted subscription code has plan_id>0 and zero quota. This exercises release-generated redemption table schema, not full release application startup.
- Baseline check: `git archive HEAD` (checkpoint commit `68199ed82`) to `/tmp/newapi-redemption-baseline.*` and `go test ./controller -count=1 -timeout=180s` produced the same OAuth/domain login, registration, and quota-cache failures as the changed tree (one extra intermittent security-account failure occurred in the changed run). These failures were not introduced by this redemption change; full controller suite still is not green. Local database servers, Homebrew installations, Docker Created containers and temporary scripts/directories are retained pending explicit user cleanup consent; do not touch pre-existing `.pi/npm/node_modules/`.
- Final review follow-up: `trellis-check` revised the real-DB matrix to refuse non-test or populated databases, unregister global DB/database-type state after each dialect, and clean only fixture tables; added real three-dialect MaxPurchasePerUser rollback case and enabled editing unused disabled/expired codes in `web/src/features/redemption-codes/components/data-table-row-actions.tsx` with `__tests__/redemptions-row-actions.test.tsx`. `go test ./model -count=1 -timeout=240s` PASS with private Unix-socket MySQL/PostgreSQL DSNs, `go test ./controller -run 'Test(TopUp|Redemption)' -count=1 -timeout=120s` PASS, `go build ./...` PASS; frontend typecheck, 4 affected Vitest files (29 tests), build and `git diff --check` PASS. `trellis-check` did not re-run the release archive/upgrade script after test-only and row-action changes; current `AutoMigrate` production code unchanged since those checks. MySQL 5.7.8/PostgreSQL 9.6 minimum versions and full old application startup upgrade are not exercised; no new version-specific SQL was introduced. Full controller baseline failures remain as noted above.
