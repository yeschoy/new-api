# Current-state research (2026-09-24)

> Scope update: the customer now wants only a read-only interval/reference report. Older refund-confirmation and wallet-evidence recommendations below are historical, not an implementation mandate. Optional `LOG_DB` consumption records cannot prove complete intervals; matching the final wallet balance alone cannot validate their time distribution. The cashback/campaign/payment-evidence observations remain relevant; where observations say signed Epay Money was discarded, current branch code has since changed.

- Current reward creation (`model/cashback_rewards.go`): online success calls `CompleteTopUpCashbackTx` inside payment transaction; it returns before creating either direction if `InviterId` is absent/invalid, then generates `inviter` and `invitee` rows with `(top_up_id,direction)` uniqueness. Both initially `pending/frozen`, the latter currently means the paying user.
- `model/cashback.go` validates `InviterID > 0` and `InviteeID != InviterID` for every direction; `model/cashback_state.go:cashbackHardBlockReasonTx` requires the referral relation for both directions. `model/cashback_risk.go` computes common evidence plus inviter-specific risk; no-inviter support needs direction-sensitive validation, hard-blocks and risk, not only reward creation.
- `setting/operation_setting/cashback_setting.go`, `model/cashback_config.go`, `controller/cashback_config.go` and `web/src/features/system-settings/types.ts` own the complete config replacement. `first_enabled_at` is immutable and global, not a repeatable campaign boundary. Preserve wire keys `invitee_*` for existing clients/Option rows while relabeling the UI as paying-user cashback.
- `model/cashback.go:InsertOnlineTopUp` persists `CashbackOrderContext` in the order creation transaction with an order-local activation decision; `CompleteTopUpCashbackTx` checks that decision and payment-time setting. An order-local campaign ID must similarly survive across the payment boundary to avoid attaching an old order to a new campaign in the same second. Keep source restrictions: provider callback/verified return only, never admin manual.
- Automatic approval can be represented by `approved/frozen` at creation, with explicit auditable evidence distinct from `reviewed_by` admin. New user requirement: an independent default-on switch makes auto-approved paying-user rewards immediately issuable after verified payment, bypassing T+N only. If off, maturity remains T+N; manual approvals still use T+N. Payment verification, limits, debt/incident blocks, reconciliation, quota fence and unique mutation remain mandatory. Existing top-up paths in `model/topup.go` hold a payer quota fence until function exit, followed by a >=10-second cooldown (`model/user_quota_cache_fence.go`); naïve post-commit `IssueCashbackReward` cannot reacquire it immediately. Plan a transaction-bound issuance that reuses the held fence and does not call global `DB` inside a single-connection SQLite transaction. Failed immediate issuance must roll back the whole verified-payment transaction for provider retry, not silently grant cashback without evidence.
- Per-user per-campaign positive reward count belongs to the same transaction as reward creation. Existing `lockCashbackUsersTx` locks beneficiary rows in sorted order before daily cap reads and is the likely serialization point for count queries; unique `(top_up_id,direction)` prevents callback duplicates. Limit only the paying-user direction; inviter eligibility remains its own branch.
- Historical rewards have no campaign reference. New campaign/context/reward fields must support legacy rows without forced backfill. New campaign model must be registered in normal and fast migration lists (inspect `model/main.go` and fast list) and migration must pass SQLite, MySQL and PostgreSQL fresh/upgrade/repeat checks.
- Refund/accounting research: `model/cashback_state.go:HandleCashbackIncident` handles one order by recovering its reward then reversing principal against available `User.quota`, creating debt for shortages; no payment refund is executed. `model/quota_reserve.go` reserves one aggregate user quota in Redis or DB without source allocation; `model/user.go` has direct increases/decreases and affiliate-to-wallet transfer; `model/redemption.go` credits quota-code redemption; `model/subscription.go:PurchaseSubscriptionWithBalance` directly deducts wallet quota. Thus a chronological per-topup refund ledger cannot be reconstructed from current `User.quota` alone. User's examples resolve under per-topup FIFO tranches (old purchase -> old gift -> next purchase): 35 and 125. Delayed gift is different: user confirmed it only joins the spend order at actual issuance, with no retroactive consumption reassignment (old paid100, new paid50, spend105, old gift10 then issued => refundable new paid45). User approved no automatic reconstruction of pre-ledger balance; old balance requires manual review. Only verified online-payment purchase is refundable; other grants are nonrefundable. User now prefers computing FIFO attribution at refund time with minimal changes to normal billing/existing tables. This may work from complete ordered credit/special-reversal evidence plus an authoritative current quota, but the exceptions are not yet proved.
- Refund-time evidence caveat: `model/log.go:RecordConsumeLog` may be disabled (`LogConsumeEnabled`) and `createLog` failure only logs an error; logs live on `LOG_DB` (potentially ClickHouse). `model/utils.go:batchUpdate` aggregates wallet deltas by user before DB flush; `model/quota_reserve.go:TryReserveUserQuota` may spend from Redis first. These logs and an unflushed DB balance cannot safely stand in for a transactional money ledger. `model/cashback_state.go:HandleCashbackIncident` records source-specific reward/principal recovery mutations and debt, not an ordinary anonymous spend; reconciliation must distinguish them. `service/funding_source.go:WalletFunding` also reserves/pre-consumes quota and may refund or adjust a reservation using `IncreaseUserQuota`, which is not a new refundable grant. During an in-flight request the current balance can understate final remaining funds; simply recording every positive wallet delta as a new grant is incorrect. An on-demand formula is a hypothesis to verify, not an established implementation.
- Additional grant-path inspection (planning only): `model/user.go:Insert` / `InsertWithTx` initializes registration and invitee welcome quota; `model/topup.go:creditTopUpQuota` credits verified top-ups; `model/cashback_ledger.go:CashbackQuotaMutation` is main-DB evidence for reward issue and actual recovery; `model/redemption.go:Redeem` leaves a used redemption record; `model/checkin.go` grants check-in quota (SQLite uses a separate create + increase with compensating delete); `model/user.go:TransferAffQuotaToQuota` raises wallet quota without a durable transfer row; `model/user_quota_adjustment.go:AdjustUserQuota` supports add/subtract/override but returns a snapshot rather than persisting a main-DB adjustment event. A smallest-change credit evidence side table would need transactional integration at these low-frequency grant boundaries, not at ordinary API consumption; raw `LOG_DB` is not enough.
- On-demand FIFO proof sketch is limited to ordinary spending: if all genuine grants (including an activation-time nonrefundable opening balance) are ordered and durable, then `sum(grants) - authoritative_current_quota` is total **net** spent. Allocate that total across grant segments in order to derive remaining purchased quota; ordinary positive billing settlement/refund deltas cancel earlier reservations rather than becoming new grants. This requires quiescent/identifiably settled wallet funding and special clawback/refund evidence; a request can reserve quota before a cashback fence and settle afterward. Existing fence/batch-pending state alone has not been proved to identify a still-running API request. Do not claim exact auto quotes during that window. For source-specific incidents, debt, unknown adjustments or unverifiable credit integrity, user approved fail-closed manual reconciliation instead of guessing.
- Cash versus quota units are not interchangeable: `controller/topup.go:getPayMoney` multiplies requested amount by configured price, group ratio and optional preset discount; `TopUp.Money` stores payment money while `CashbackOrderContext.CreditedQuota` and `User.quota` use wallet units. Different providers also normalize `TopUp.Amount`/credited quota differently. The user's 35/125 examples implicitly use 1:1 prices. User confirmed per-order actual-paid proration capped at the original payment (100 credited purchase, 90 paid, 50 unused => 45 cash). Important verified-source gap: `controller/topup_stripe.go:fulfillOrder` receives signed `amount_total`/`currency`, but sends only reference/customer to `model.Recharge`; `TopUp.Money` is set before checkout from `GetChargedAmount` while Stripe Checkout permits promotion codes. The signed actual paid amount is currently logged only after credit and is not durable main-DB refund evidence. Likewise `controller/topup_creem.go:handleCheckoutCompleted` receives signed `amount_paid`/`currency` but sends only reference/customer to `model.RechargeCreem`; `controller/topup_waffo_pancake.go` receives signed amount/currency (see `service/waffo_pancake.go:VerifyConfiguredWaffoPancakeWebhook`) but calls `model.RechargeWaffoPancake(tradeNo)` without them. For Epay, `github.com/Calcium-Ion/go-epay/epay.VerifyRes` includes signed `Money` but no currency; `controller/topup.go:EpayNotify` passes only signed trade number/type to `model.RechargeEpay`, not `Money`. Epay merchant configuration would still need an explicit CNY assumption/check before treating signed `Money` as yuan. Waffo actual amount/currency provenance still needs a full audit. User narrowed automatic cash quoting to Epay orders with independently confirmable CNY settlement; other gateways retain cashback eligibility, contribute wallet credit segments to FIFO attribution, and require manual cash refund reconciliation. Epay's signed `Money` can provide the amount once persisted, but currency is not in the protocol response. An order lacking reliable actual-paid/CNY evidence must not receive an automatic cash quote.
- Existing frontend `cashback-settings-form.tsx` and `cashback-settings-section.tsx` own Root settings; `web/src/features/cashback` owns Admin table/filter/summary/detail. Reuse `SettingsSwitchItem` / `SettingsSwitchField`, form inputs, existing alert/dialog and list patterns. Existing high/severe risk filters and summary already display them; rename `Invited user` direction label without creating third direction. Seven locale files need sync.
- Existing test hubs: `model/cashback_test.go` for state, `model/cashback_integration_test.go` for MySQL/PostgreSQL (via `TEST_MYSQL_DSN` and `TEST_POSTGRES_DSN`), `controller/cashback_config_test.go` for config, `web/src/features/system-settings/billing/__tests__/cashback-validation.test.tsx` for UI. Existing integration test gives isolated schema/prefix. No DSNs exported in this session; Docker CLI available but daemon not running, while local `mysqld`, `initdb`, `pg_ctl` binaries exist. Provision real databases before claiming DB compatibility; record exact versions/results. Old-release representative upgrade fixture needs separate verification.
- Applicable rules: `.agents/rules/billing.md` (read in full), `.trellis/spec/backend/referral-recharge-cashback.md`, `AGENTS.md`, `web/AGENTS.md`, `.agents/skills/shadcn-ui/SKILL.md`, `.trellis/spec/guides/cross-layer-thinking-guide.md` and `code-reuse-thinking-guide.md`.

## Stage-1 feasibility gate — read-only source inspection

**Conclusion:** ordered durable grants + one authoritative final wallet balance suffice for *unreversed, nonnegative, settled FIFO spending*, including late cashback grants, but do **not** establish the requested chronological attribution for all normal billing histories. No refund product code should be written on the assumption that a request-lifecycle marker alone closes the gap. This is a source-level finding plus deterministic arithmetic, not a three-database runtime verification.

### Conditional proof and arithmetic

Let `G=[g1,...,gn]` be all committed, actually available quota segments in wallet-credit order, including an opening nonrefundable balance, and let `Q` be an authoritative nonnegative final balance. If every other committed wallet mutation is a plain FIFO spend, there is no unsettled reservation, special withdrawal, or unexplained adjustment, then `S=sum(G)-Q` units have been spent. The remaining units are exactly the suffix of `G` with total size `Q`: spending removes a prefix and appending a grant adds a suffix. Induction over those two operations shows that intermediate ordinary spend timestamps need not be stored. A same-transaction purchase and instant gift must get stable purchase-before-gift order; a delayed gift is appended only when `issueLockedCashbackRewardTx` actually credits the wallet (`model/cashback_state.go`), **not** at order/payment time. Segment order must come from one committed serialized sequence, not Unix seconds or the order of inserts made before acquiring the user wallet lock.

Arithmetic checked with a local, file-free Python calculation: A, `[old purchase 100, gift 10, new purchase 50, gift 5]`, final `Q=40` leaves `new purchase 35, gift 5`; B, same grants and `Q=140`, leaves old purchase 75 + new purchase 50 and gifts 15; delayed gift `[old purchase 100, new purchase 50, old gift 10]`, `Q=55`, leaves new purchase 45 + gift 10. Other gateway grants and admin/redeem grants must also occupy nonrefundable segments at their *actual* credit position; a missing grant can wrongly increase the reported remaining Epay purchase. An opening mixed balance may only be a nonrefundable starting segment, never an automatically refundable historical purchase. Example verified CNY 90 paid / 100 purchased / 50 unused yields 45 paid units; use integer cents and cumulative target differences for partial refunds, never independently round every retry.

### Counterexample: later reversal of an earlier reservation

The same ordered grants and final balance can have different historical purchase attribution. History H1: grant old purchase 100, grant new purchase 50, then spend 40 -> old 60/new 50, balance 110. History H2: grant old purchase 100, reserve/spend 100 from old, grant new purchase 50, spend 40 from new, **then refund the first reservation 100** -> old restored 100/new 10, balance 110 (or a new late restoration segment linked to the old paid order). The proposed `sum(grants)-Q=40` algorithm outputs old 60/new 50 for **both**. In H2 it retroactively reallocates 40 units of the later spend from new to old. The agreed late-gift policy disallows retroactive reallocation for a *late grant*; the user has now confirmed that *temporary wallet reservation and its later refund* may be netted at final settlement and may rebase subsequent consumption; this does not change the late-gift rule. Treating the reservation refund as a fresh generic *nonrefundable* grant also discards the verified original purchase's refund eligibility. This was a material accounting semantic (now resolved in favor of final net consumption), not a stale-balance-only race: **waiting until the session ends does not restore the lost ordering**. With that policy, the refund-time formula may return old 60/new 50 only after the reservation has genuinely settled/refunded; a per-user unsettled lifecycle signal must fail closed before then. Recording an activity marker is necessary but not by itself sufficient to establish durable batch and legacy/task wallet evidence. This issue also applies to `WalletFunding.Settle(-delta)` and task/legacy wallet refunds, not only failure paths.

Source: `service/funding_source.go` `WalletFunding.PreConsume/Settle/Refund`, `service/billing_session.go` `Settle/Reserve/Refund` and `service/billing.go` `SettleBilling`. `BillingSession.Refund` sets an in-process flag *before* launching `gopool.Go`; the wallet refund uses a non-idempotent `IncreaseUserQuota` with no durable request record or retry. A process crash or failed async credit cannot be inferred from final `User.quota`; even a durable in-flight marker would need a terminal `failed/reconcile` state and operator disposition, never a timeout that asserts success. Trusted wallet sessions can initially reserve zero and later charge; register them before reporting the wallet settled. The legacy `SettleBilling` fallback and `PostConsumeQuota` (`service/quota.go`) and task billing (`service/task_billing.go:taskAdjustFunding`, `service/midjourney.go:RefundMidjourneyQuota`) also write wallet credits/debits outside the proposed `BillingSession` lifecycle. A single `BillingSession` hook is not universal. The `model/subscription.go:PurchaseSubscriptionWithBalance` debit is ordinary FIFO spending but requires the same lock/snapshot boundary; subscription-funded requests themselves do not debit the wallet.

### Evidence inventory and fail-closed cases

| Path | Durable main-DB evidence now | Minimum safe treatment for automatic cash quote |
| --- | --- | --- |
| Five provider completions, `model/topup.go` | Successful `TopUp`, `CashbackOrderContext.CreditedQuota` for covered orders; no per-wallet-credit sequence; manual completion credits too | Write one unique credit event for **every actual** wallet top-up in the same payment transaction; only verified online events are refundable; order by user-serialized event sequence, not `CompleteTime` seconds. Missing old context/evidence => manual. |
| Reward issue/recovery and principal incident, `model/cashback_state.go`, `model/cashback_ledger.go` | Unique `CashbackQuotaMutation.EventKey` records actual issue and actual recovered amount; context stores targets/debts | Add ordered credit evidence on *actual issue*. Incident does not debit its own tranche exclusively: `reserveCashbackRecoveryQuota` takes whichever wallet balance exists, then records a target/source-linked recovery, possibly creating debt. Existing incident on an account or overlapping manual cash refund => manual until a proven correction model exists. Debt resolution only clears debt, not credits wallet. Never double-run `HandleCashbackIncident` for manual payout. |
| Registration (`model/user.go:Insert/InsertWithTx`), redeem (`model/redemption.go:Redeem`) | User starting quota and consumed redemption code; `LOG_DB` invite logs best effort | Atomic nonrefundable opening/new-user grant and unique redeem grant in main DB; OAuth registration uses `InsertWithTx`; invitation affiliate credit stays in `aff_quota` until transferred. |
| Check-in (`model/checkin.go`), affiliate transfer (`model/user.go:TransferAffQuotaToQuota`) | Checkin row; transfer only new user balances, no durable transfer event | Add unique nonrefundable event in same transaction as wallet credit. SQLite check-in currently does separate insert + `IncreaseUserQuota(...,true)` + compensating delete: no atomic event guarantee until that boundary is changed, or fail closed for affected account. Affiliate transfer's full-`User` `Save` and Redis cache behavior need separate review. |
| Manual add/subtract/override (`model/user_quota_adjustment.go`, `controller/user_quota.go`) | Transactional before/after returned, then best-effort `LOG_DB` audit; no main-DB adjustment record | Add an atomic uniquely keyed main-DB event for ordinary add (nonrefundable); subtract and override are source-ambiguous, so mark the account permanently `manual_reconciliation` unless a precise business attribution rule is approved. |
| API/task, failed reservation, balance subscription (`service/*`, `model/subscription.go`) | Aggregate `User.quota`, optional asynchronous/disableable `LOG_DB` consume logs; some task state; no complete per-wallet spend log | Only a *fully settled* ordinary spend can use the suffix proof. Model and test all reversible wallet paths; balance subscription has its own transactional order, but is not a grant. Generic positive `IncreaseUserQuota` also means reserve rollback, not necessarily a new grant. |

`model/log.go:RecordConsumeLog` skips when `LogConsumeEnabled=false`, `createLog` writes `LOG_DB` separately and failure does not stop wallet mutation. These logs cannot fill the missing evidence. `model/quota_reserve.go` can reserve Redis balance first; `model/utils.go` queues per-user *aggregated, in-process* batch deltas and later flushes them, while `model/user_quota_cache_fence.go` keeps an **expiring** Redis pending marker. An active marker blocks snapshots while present, but after a crash between cache update and durable DB flush, the queue is gone and the marker/hash can expire; a later DB read can be stale with no durable proof that a delta was lost. Even without crash, a cleared batch marker says nothing about a still-running request that will refund or settle later. No-Redis mode writes wallet reserve/settle directly but still has the in-flight problem. Therefore exact quotes need a cross-instance, DB-persistent fail-closed wallet lifecycle/dirty signal and either a durable batch intent/flush acknowledgement or a verified synchronous DB path (or disable automatic quoting for all affected batch accounts). Redis TTL, cache fence and `GetUserQuota(..., true)` alone do not certify historical integrity. Quote and confirm must hold the same wallet fence plus user lock and revalidate all evidence *inside* the transaction; outside read is display-only. In particular, `model/user.go:DecreaseUserQuota` and `service/billing_session.go:reserveFunding` may drive quota negative on positive post-settle adjustments; a later top-up can hide that debt from a nonnegative final balance. Persist an untrusted/debt history signal or fail closed, not just `Q>=0`.

Other gate cases: a refund followed by a new purchase and consumption must use explicit committed removal/restoration events and wallet-locked re-quote, not `HandleCashbackIncident`; partial recovery of an already-spent old purchase can debit a newer purchase while recording a recovery against the old order, so its `CashbackQuotaMutation` cannot simply be subtracted from the old segment. Both go to manual review until their source/target semantics are proven. Same-second credits must be serialized by user-wallet row and assigned an unambiguous event order; second-resolution `CompleteTime`, `IssuedAt`, and `CreatedAt` do not suffice. An in-flight batch mutation means `retry_later`; lost/unknown batch history, negative wallet, an admin override, or source-specific debt means `manual_reconciliation` with no cash amount. A manual refund record needs a unique request/idempotency key, atomic wallet reservation and per-order paid-cents cap, plus explicit `pending_payout/paid/failed_manual_resolution` states: external cash transfer is not in the DB transaction and must never automatically repeat a wallet debit.

### Epay money and CNY provenance

`github.com/Calcium-Ion/go-epay/epay` v0.0.4 `epay/order.go:VerifyRes` exposes signed `Money` as a **string**, along with trade number/type/status, but no currency. `Verify` checks the signature; `epay/util.go:GenerateParams` includes nonempty `money` in the signed set. Both `controller/topup.go:EpayNotify` and `controller/topup_return.go:EpayBrowserReturn` currently pass only verified trade number/type to `model.RechargeEpay*`: signed `Money` is discarded before `model/topup.go:rechargeEpayWithSource` writes `TopUp` success/`CashbackOrderContext`, so *even a mismatch currently credits the order*. Pass the verified amount through **both** routes, parse positive decimal with at most two fractional digits to integer cents without float rounding, compare with the checkout amount encoded to two decimal places (`RequestEpay` sends `strconv.FormatFloat(payMoney,'f',2,64)` while `TopUp.Money` stores the unrounded `float64`; ideally persist expected integer cents at order creation), then store signed cents, gateway/merchant identity, order ID and an immutable CNY-attestation reference atomically with payment credit. Mismatch must roll back wallet and cashback, not merely suppress the quote. Other payment channels remain cashback-eligible and generate FIFO wallet-credit events but must not be auto-quoted as CNY.

`setting/operation_setting/payment_setting_old.go` and `model/option.go` show mutable `PayAddress`, `EpayId`, `EpayKey` options updated independently through `controller/option.go:UpdateOption`; `Verify` uses the *current* key. A Root assertion of CNY receipts is an auditable **business attestation**, not currency evidence in the callback. Bind an immutable confirmation to the merchant/gateway configuration generation and the order's verified payment transaction, invalidate on any gateway/merchant/key change in the same authoritative configuration boundary, and never apply a new assertion to a historical order. A separate volatile Boolean or non-transactional comparison of in-memory globals cannot prove this across concurrent updates/multiple nodes. Without documented actual CNY settlement confirmation, or for old orders lacking signed amount snapshot, show `manual_reconciliation` rather than a numeric CNY quote. `TopUp.Money` is expected checkout price (price/group/discount), not wallet quota or independently verified actual paid; other gateways may discount after checkout.

**Stage-gate update:** User confirmed final-net rebasing for temporary reservation/refund; H1/H2 now share the intended 60/50 attribution. This does **not** prove wallet evidence integrity: old mixed balance, source-specific incidents/debt/override and unsupported currency remain excluded from numeric quotes; a single active-session marker does not cover legacy/task paths, asynchronous failures or lost in-memory batch deltas. Proceed only with fail-closed handling and focused tests for every supported wallet path. If the minimal evidence mechanism cannot certify normal accounts, pause and re-plan rather than claim accurate automatic quotes. This stage did not edit business code, provision databases or run integration/concurrency tests.


## Research-agent follow-up: exact Epay CNY quote gate (read-only)

**Recommendation / scope:** The suffix-of-ordered-credits calculation is conditionally sound for a *certified, quiescent wallet with ordinary net FIFO spending*; it is not a proof for all existing accounts. Proceed only with a per-user durable evidence boundary, atomic order-credit/bonus events, and a separate payment attestation. A quote API must return `retry_later` for live/unsettled funds, `manual_reconciliation` (no numeric amount) for missing or contaminated history; never reinterpret Redis cache or optional logs as a money ledger. If the required coverage cannot fit the approved centralized wallet-lifecycle and low-frequency credit/exception entries, return to planning rather than weakening the guarantee. Current branch already has Epay signature-amount **comparison** (`model/topup.go:epayAmountCents`, `rechargeEpayWithSource`, both `controller/topup.go:EpayNotify` and `controller/topup_return.go:EpayBrowserReturn` pass `Money`); it does **not** persist signed cents, CNY evidence or funding provenance. Do not describe the earlier “signed Money is discarded” observation above as the current branch state.

**Concrete proof obligations and smallest safe boundary:**

1. New per-user main-DB boundary must atomically capture old mixed `User.quota` as a nonrefundable segment, without retroactive refundable history; serialize *all* actual wallet credits against that user and append a unique ordered event in the **same DB transaction**. `model/topup.go:creditOnlineTopUpWithCashbackTx` handles purchases, `model/cashback_state.go:issueLockedCashbackRewardTx` handles gifts at actual issuance, `model/user.go:Insert/InsertWithTx`, `model/redemption.go:Redeem`, `model/checkin.go:userCheckinWithTransaction/userCheckinWithoutTransaction`, `model/user.go:TransferAffQuotaToQuota`, `model/user_quota_adjustment.go:AdjustUserQuota` require explicit source treatment. SQLite check-in is currently separate create/credit/compensating delete; it cannot establish atomic evidence as-is. Admin subtract/override, direct unclassified wallet changes, cashback recovery/incident (`model/cashback_state.go:HandleCashbackIncident` debits *available wallet*, not necessarily its named tranche), debts and existing manual refunds must poison the automatic path until reconciled; an `issue` mutation proves only the cashback gift, not complete wallet history. Credit sequence must be ordered under the user DB lock/transaction, not timestamp or an ID reserved before the lock. Quote/confirm recheck inside that same serialized money boundary and cap per-order cumulative paid cents.
2. Register a durable wallet *open/terminal/failed-needs-review* marker **before the first possible wallet mutation**, including zero-reserve trusted sessions and extra `Reserve`; close only after funds actually settle or async refund actually commits. `service/billing_session.go:NewBillingSession/preConsume/Reserve/Settle/Refund` can start at zero and `Refund` sets `refunded` before `gopool.Go` calls `WalletFunding.Refund` (`service/funding_source.go` non-idempotent credit). Process crash/failed credit cannot be inferred from a `settled`/`refunded` in-memory bool; no TTL may silently mark it clean. `service/billing.go:SettleBilling` falls back to `service/quota.go:PostConsumeQuota` without a session, also used in realtime and `service/violation_fee.go`. `service/task_billing.go:taskAdjustFunding/RefundTaskQuota/RecalculateTaskQuota` and `service/midjourney.go:RefundMidjourneyQuota` can mutate funds long after the submitting request. In `service/task_polling.go` the task becomes terminal via `UpdateWithStatus` **before** `finalizeTerminalTask` settles/refunds, and `RefundTaskQuota` updates wallet **before** `task.UpdateQuota`; `status=success/failure` alone is not a settlement certificate. Either cover these via the common wallet mutation/lifecycle boundary and durable task evidence or disable auto quote for affected users (no proof of absence from an empty session table alone). A failed/ambiguous compensation must stay dirty for manual intervention.
3. `model/quota_reserve.go:TryReserveUserQuota` may debit Redis before DB; `model/user.go:IncreaseUserQuota/DecreaseUserQuota` in batch mode only queue deltas. `model/utils.go:batchUpdate` swaps maps in RAM, aggregates by user and logs/requeues on DB error; `model/user_quota_cache_fence.go` Redis pending members expire. `model/quota_reserve_test.go:TestSharedBatchMarkerBlocksCrossInstanceColdCacheRecovery` verifies a *live* pending marker, not persistent history after process crash/TTL. A later `BatchUpdateEnabled=false` does not repair an earlier lost batch delta. Therefore refuse all accurate quotes for any user whose post-boundary wallet ever ran in unproven batch mode, even after restart or mode switch, unless durable per-user intent/ack and recovery are implemented/tested. No-Redis mode still requires durable open/terminal markers and locked DB snapshot. A past negative balance hidden by later top-up (`BillingSession.reserveFunding`, `WalletFunding.Settle`, `model/user.go:DecreaseUserQuota`) must remain permanently flagged until reconciled, not passed just because current Q≥0. Optional `model/log.go:RecordConsumeLog` writes to `LOG_DB` and is not such evidence.
4. Epay: retain strict signed-amount-to-checkout-cents rejection already in `rechargeEpayWithSource`, additionally snapshot verified `paid_cents`, provider/trade ID and an immutable *current-merchant CNY attestation generation* atomically with payment credit. `controller/option.go:UpdateOption` → `model/option.go:UpdateOption` can independently change `PayAddress`, `EpayId`, `EpayKey`; confirmation and invalidation must be serialized with updates and checkout/payment evidence across instances. A signed Epay amount has **no currency**. A merchant's CNY assertion is an auditable external fact, not cryptographic currency proof. Old orders without bound proof, or Stripe/Creem/Waffo orders, contribute ordered wallet segments (other gateways’ purchased funds may need manual cash reconciliation), but cannot acquire automated CNY quotes by later confirmation. On confirmation, hold the wallet fence and DB user lock, reject active/dirty state, re-read ordered evidence and balance, reserve/refund idempotently with pending external payout state; payout remains manual. `model/user_quota_adjustment.go:AdjustUserQuota` currently writes balance without acquiring the quota mutation fence and applies cache delta after commit; it must not race that critical section.

**Product policy still unresolved (not an assertion that current code violates a chosen rule):** Requirements demand a **partial** refund and that refunded purchase *and associated gifts* cannot subsequently be used; the design says cancel unissued rewards and debit issued rewards, but does not define the gift proportion to cancel/claw back on a partial refund, especially the inviter's reward in a **different wallet** and already-spent instant gifts. Current `HandleCashbackIncident` scales principal and recovers reward by order, potentially creating debt; applying that to a wallet FIFO cash refund would double-charge. Before enabling automated **confirmation**, owner must decide whether partial cash refund forfeits the whole gift, a proportional gift (and rounding rule), or blocks unless full gift can be recovered; likewise whether an inviter's insufficient balance blocks cash payout or creates a separately audited debt. A read-only cash quote can fail closed for such cases but cannot claim the confirmation flow satisfies “关联赠送额不得二次使用” without that decision. This does not require changing the already approved rule that temporary API reservation refunds are netted while genuinely late gift credits are not backdated.

**Checks performed:** Read current PRD/design/implement, full `.agents/rules/billing.md`, existing research and source listed above; traced grep call sites for direct wallet writes/legacy/session/task/provider paths; ran file-free deterministic Python FIFO arithmetic: A=35, B=125, late gift=45, reservation net old/new=60/50, 90 CNY/100 paid-quota with 50 left=45 CNY. No backend/integration tests, DB instances, live payment or merchant settlement verification in this research pass; existing dirty working tree was not modified outside this research file.
